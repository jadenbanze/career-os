# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Secrets & credentials (this app uses the OS keychain — never commit these)
.env
.env.*
*.pem
*.key
*.p12
*.keystore
*.jks

# Local databases & debug dumps (live in app data dir, but guard against copies)
*.db
*.db-shm
*.db-wal
*.sqlite
github-last-sync.json

# Tauri updater signing keys
*.tauri
